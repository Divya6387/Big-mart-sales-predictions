# Big-mart-sales-predictions
# Import Libraries 
# pandas → for handling datasets (tables)
# numpy → for numerical operations
# Load training & testing data
# test1 → backup copy (used later for final output)
# Check Shape - Shows number of rows & columns
# Check Missing Values
# Handle Missing Values + Mapping Convert text → numbers (Small=1, Medium=2, High=3) Item_Weight → mean Outlet_Size → median
# Drop Unnecessary Column
# Convert Categorical → Dummy Variables Converts categories into multiple binary columns (One-Hot Encoding)
#
# Model Evaluation MAE → average error MSE → squared error RMSE → most important (lower = better) {metrics}
